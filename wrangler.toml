# Cloudflare (Pages sobre Workers) — sitio estático, sin paso de compilación.
#   npm run deploy   -> publica el repo entero
#   npm run preview  -> servidor local en http://localhost:8000
#
# Qué NO se sube está en .assetsignore.
name = "urus-traffic"
compatibility_date = "2026-09-01"

[assets]
directory = "."
