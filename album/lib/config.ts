/** Una sola colección activa en la v1; el modelo soporta varias desde el día uno. */
export const COLLECTION_SLUG = process.env.NEXT_PUBLIC_COLLECTION_SLUG || 'liga-2025-26';

/** Proporción real de una carta: 63 × 88 mm. */
export const CARD_ASPECT = '63 / 88';

export const APP_NAME = 'Hoja';
